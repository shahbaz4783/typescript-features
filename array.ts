// To initilize empty array, give annotation
const cars: string[] = [];

// To initalise 2D array
const mobiles: string[][] = [];

// Type inference
const pets = ['dog', 'cat', 'goat'];

const dates = [new Date(), new Date(), new Date()];

// Uninon in Arrays
const impDates: (Date | string)[] = [];

const numStr: (number | string)[] = [];
